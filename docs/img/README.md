# Screenshots

This directory holds the screenshots referenced by the top-level README. The images are placeholders until captured. Add the following files here, sized for readable inline display (roughly 1600 pixels wide, PNG):

- `investigation.png` the Investigation console showing a cited, multi-hop answer with an expanded raw log line.
- `graph.png` the Knowledge graph tab with the interactive Cytoscape graph and the entity legend.
- `timeline.png` the Timeline tab with the dense, filterable event table.
- `the-turn.png` The Turn tab showing the before-and-after conclusion side by side after the verdict flips.
- `demo.gif` a short recorded walkthrough (case picker → ASK → graph → The Turn) for the README's Demo section.

Already committed: `logo.svg` — the KoshurLock Holmes mark (magnifying lens over a connected entity graph), embedded at the top of the top-level README.

Capture guidance: use the seeded demo case so the views are populated, keep the dark theme, and crop to the content area.
